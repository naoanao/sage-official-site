# Growl — セルフサーブ連携 App Review 提出パッケージ

最終更新: 2026-06-14 ／ 対象: Meta（Facebook）App Review ＋ TikTok App Review
目的: 一般ユーザーが**自分のMeta広告アカウント／TikTok**を自分で接続（OAuth）できるようにする（＝代行を介さないセルフサーブ）。

> ⚠️ 重要な前提: App Review が必要なのは「アプリに role を持たない一般ユーザー」がアプリを使う場合。現在の代行モデル（なおの System User トークンで運用）は App Review **不要**で既に稼働中。本パッケージは「将来セルフサーブを開く」ための準備物。

---

## 0. 提出前チェックリスト（これが揃わないと審査に出せない）

- [ ] **ビジネス認証（Business Verification）完了** — ads_management の Advanced Access に必須。数営業日かかる。Meta Business Suite → Security Center で書類提出。
- [ ] **プライバシーポリシーURL（公開・実在）** — 例: https://growl-ai.com/privacy （✅実在を確認済み）
- [ ] **利用規約URL** — 例: https://growl-ai.com/terms （✅実在を確認済み）
- [ ] **データ削除URL/手順** — ユーザーがデータ削除を要求できるエンドポイントまたは説明ページ（Metaは「Data Deletion Callback」or 手順URLを要求）。⛔ **未実装 → 要作成**（例: /data-deletion ページ＋手順）
- [ ] **有効なOAuthリダイレクトURI** — App設定の Valid OAuth Redirect URIs に本番URLを登録（例: https://growl-ai.com/api/auth/meta/callback）。
- [ ] **アプリが Live モード** — Development ではなく Live に切替。
- [ ] **アプリアイコン・カテゴリ・説明** を設定。
- [ ] **セルフサーブOAuthフロー自体の実装** — ⛔ 現状コードは代行（System User）方式で、ユーザー向けMeta OAuthフローは未実装/撤去済み。審査用に最小フロー（接続→コールバック→トークン保存）の再実装が必要。

---

## 1. Meta: 申請する権限と「そのまま貼れる」justification

> 各権限ごとに「何に使うか・なぜ必要か・ユーザー利益」を英語で簡潔に。スクリーンキャストで該当操作を必ず映すこと。

### ads_management（広告の作成・管理・停止／write）
**Justification (EN, paste-ready):**
"Growl is an AI marketing assistant for small businesses. After a user connects their own Meta ad account, Growl creates and manages ad campaigns, ad sets, and ads on their behalf based on AI-generated copy and targeting the user reviews and approves. We also pause or adjust budgets automatically to prevent wasted spend. ads_management is required to create campaigns, upload ad creatives, and update budget/status on the user's own ad account. No data from accounts the user does not own is accessed."

### ads_read（成果の取得／read）
"Used to read campaign insights (spend, impressions, clicks, CTR, CPC, conversions) from the user's own ad account so Growl can show performance reports and let the AI optimize future campaigns."

### business_management（任意・複数拠点/BM運用時）
"Used to list the businesses and ad accounts the user manages, so the user can select which account Growl should operate on. Only the user's own businesses are accessed."

### pages_show_list / pages_read_engagement（広告にPageが必要なため）
"Meta requires a Facebook Page to run ads. pages_show_list lets the user pick which of their own Pages the ad runs under; pages_read_engagement is used to display basic Page info during selection."

### leads_retrieval（Lead Ads を使う場合）
"When the user runs Lead Ads, this permission lets Growl retrieve the leads generated by the user's own ads so they can be delivered to the user (email/CRM). Requires accepting Lead Ads Terms in the ad account."

> 📌 注意: ads_management の **Advanced Access** には Business Verification 必須。Standard Access（自分の資産のみ）は審査なしで使えるが、一般ユーザーの資産には使えない。
> 📌 2026-05-04 より旧「Ads Management Standard Access (AMSA)」は **「Marketing API Access Tier」** に改称。Marketing API の利用枠（tier）はここで管理。

---

## 2. Meta: スクリーンキャスト台本（審査員に見せる動画）

1. ログイン済みの Growl で「Connect Facebook」を押す。
2. Meta OAuth ダイアログが出て、上記の権限に同意する様子を映す（権限名が画面に出ること）。
3. 接続後、ユーザーが自分の Page / 広告アカウントを選択する画面。
4. AIが広告コピー・画像・ターゲティングを生成 → ユーザーが確認 → 「配信」。
5. （ads_management）キャンペーンが実際に作成される様子。
6. （ads_read）成果レポート画面（spend/CTR等）が表示される様子。
7. （leads_retrieval：使う場合）取得したリードがユーザーに届く様子。
8. 設定からアカウント連携を**解除**できる様子＋**データ削除**手順。

> 音声 or 字幕で各操作を英語ナレーション。1本にまとめてOK。Reviewerが「自分の資産にだけ使っている」と分かることが最重要。

---

## 3. TikTok: セルフサーブ審査メモ

- 現状: `client_key` が `sb...`（**サンドボックス**）。実ユーザーは連携を完了できない。
- 必要: TikTok for Developers でアプリを本番申請し、スコープ **user.info.basic / video.upload** の審査を通す。
- redirect_uri を本番ドメインに合わせる（現状 `growl-app.vercel.app/api/auth/tiktok/callback`。`growl-ai.com` に統一推奨）。
- スクリーンキャスト: 接続 → 同意 → Growlが生成した動画をユーザーのTikTokへ投稿 → 解除/削除、までを録画。
- プライバシー/利用規約/データ削除URLは Meta と共通でOK。

---

## 4. いま着手できる順序（推奨）

1. **データ削除ページ/エンドポイント作成**（/data-deletion）— Meta・TikTok共通で必須。AIで実装可。
2. **ビジネス認証の書類提出**（なおの作業・数日かかるので最初に出す）。
3. **セルフサーブMeta OAuth最小フロー実装**（接続→コールバック→user_meta_tokens保存）。AIで実装可。
4. スクリーンキャスト撮影（上記台本）。
5. App Review 提出（権限＋justification＋動画）。
6. TikTokも同様に本番申請。

> ⚠️ 1・3 はAIが実装できる。2・4・5・6 は Meta/TikTok ダッシュボードでのなおの操作が必須（外部審査のため代行不可）。

---

## 5. 現状の到達点（2026-06-14）

- 代行モデル（System Userトークン）は App Review 不要で**稼働中** → 今すぐ収益化できるのはこちら。
- セルフサーブは「成長レバー」として上記手順で開ける。ただし審査に数日〜数週間かかるため、**まず代行で実需を作りつつ並行申請**が現実的。

---

### 参考（2026-06時点の要件確認）
- App Review の要否: アプリに role の無いユーザーが使う場合に必須。
- ads_management: 広告の作成/管理/削除（write）。Advanced Access に Business Verification 必須。
- 旧AMSA → 「Marketing API Access Tier」へ改称（2026-05-04）。
